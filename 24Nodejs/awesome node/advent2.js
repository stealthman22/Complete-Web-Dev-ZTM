const fs = require('fs');

// Read file synchronously
const advent = fs.readFileSync('./input.txt').toString()

// Spread file into an array
const adventArray = [...advent]


const sumOfAdventArray = adventArray.filter(caract => {


})
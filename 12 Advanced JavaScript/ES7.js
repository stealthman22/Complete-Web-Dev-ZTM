//includes
const pets = ['cat', 'dog', 'bat'];
pets.includes('dog');

const string = 'letter';
string.includes('e');

const obj = {
    a: 'test',
}
obj.includes(test);

//Exponential operator
const square = x => x ** 2;
const cube = y => y ** 3;
const gaga = y => y ** 4;